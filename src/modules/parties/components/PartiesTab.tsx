import React from 'react';
import { PartiesView } from './PartiesView.tsx';

export const PartiesTab: React.FC<any> = (props) => {
  return <PartiesView {...props} />;
};

export default PartiesTab;

